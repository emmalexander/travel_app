import 'package:flutter/material.dart';

class ChartItemPage extends StatelessWidget {
  const ChartItemPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text('Chart Item Page')),
    );
  }
}
